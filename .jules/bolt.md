## 2024-05-24 - Static HTML Optimization Focus
**Learning:** The user-facing application is primarily built within the static `index.html` file, not the React `src/App.jsx` scaffold.
**Action:** Prioritize static web performance optimizations (like resource hints, `<link rel="preconnect">`) in `index.html` over React-specific optimizations for the main entry points.
